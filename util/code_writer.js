/**
 * Generate the source code header.
 * @param   {string} title the structure name
 * @returns {string}   header as gamepedia sourcecode
 */
var generateHeader = function(title) {
    return '===' + title + '===\n==== Materials ====\n';
};

var generateMaterialsTable = function(materialsData) {
    var result = '{| class="wikitable"\n|-\n';
}


module.exports = {
    generateSourceCode: function(readableData) {
        
    }
};